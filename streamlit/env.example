# Configuration GCP pour le dashboard Streamlit
# Copiez ce fichier vers .env et modifiez les valeurs selon votre configuration

# ID du projet GCP
GCP_PROJECT_ID=spark-streaming-483317

# Dataset BigQuery (par défaut: shopping_dev pour l'environnement dev)
BIGQUERY_DATASET=shopping_dev

# Table BigQuery
BIGQUERY_TABLE=orders

# Chemin vers le fichier de credentials du Service Account (optionnel)
# Si non défini, utilise Application Default Credentials (recommandé sur GCP/Cloud Run)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

